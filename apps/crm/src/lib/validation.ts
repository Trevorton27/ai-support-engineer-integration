// Re-export all validation utilities from the shared package
export {
  ALLOWED_FILE_TYPES,
  MAX_FILE_SIZE,
  validateSubject,
  validateMessage,
  validateFileType,
  validateFileSize,
} from '@repo/shared-types';
