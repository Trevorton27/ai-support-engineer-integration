{
  "name": "ai-support-engineer-integration",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"crm,tickets\" --prefix-colors \"blue,green\" \"npm run dev --prefix sample-crm\" \"npm run dev --prefix smart-ticket-system\"",
    "dev:crm": "npm run dev --prefix sample-crm",
    "dev:tickets": "npm run dev --prefix smart-ticket-system"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
